package com.dothereco.DotheReco.repository;

import com.dothereco.DotheReco.domain.Unfixed;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UnfixedRepository extends JpaRepository<Unfixed, Long> {
}
